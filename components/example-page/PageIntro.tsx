import PageIntro from "@/components/shared/PageIntro"

export default function ExamplePageIntro() {
  return (
    <PageIntro
      title="Worked Example — SWCNT–TiO₂–Re Oxygen Sensor"
      text="This is a worked example of what the ttOS Marketing Bot produces for a single invention: a full deal room replacing the static one-page non-confidential PDF that TTOs currently send. Built from the SWCNT–TiO₂–Re oxygen sensor (Wettstein et al., ETH Zürich, 2025)."
    />
  )
}
